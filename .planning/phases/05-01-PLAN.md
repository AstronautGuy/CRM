# Phase 5 Plan 1: Ad Campaigns, Tasks & Reminders Database Schema

## Objective
Define Drizzle ORM schemas for `ad_campaigns`, `crm_tasks`, and `notifications` under tenant organization isolation.

## Tasks
- [ ] Extend `src/server/db/schema.ts` with `ad_campaigns`, `crm_tasks`, and `notifications` tables.
- [ ] Include priority levels, due dates, assignee user IDs, spend, clicks, and conversion metrics.

## Verification
- Run `pnpm typecheck` to verify schema type safety.
