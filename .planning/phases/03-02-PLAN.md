# Phase 3 Plan 2: tRPC Routers for Pipelines & Deals

## Objective
Build tRPC procedures for pipeline CRUD, stage reordering/customization, deal stage movement, and summary metrics calculations.

## Tasks
- [ ] Build `src/server/api/routers/deals.ts` with procedures for:
  - Fetching pipelines, stages, and deals.
  - Creating/updating pipelines and stage configurations.
  - Moving deals between stages (updating stageId & index).
  - Calculating aggregate pipeline metrics (Total Value, Weighted Forecast, Win Rate %).
- [ ] Register `dealsRouter` in `src/server/api/root.ts`.

## Verification
- Run `pnpm typecheck` to verify tRPC API compilation.
