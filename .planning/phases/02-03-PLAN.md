# Phase 2 Plan 3: Contacts & Lead Management UI Views

## Objective
Build UI screens for viewing, searching, filtering, and creating Contacts and Companies, including contact detail pages with activity logs and CSV import modals.

## Tasks
- [ ] Create Contacts Datatable view at `src/app/(dashboard)/contacts/page.tsx` with search, lead status filter, and tag filters.
- [ ] Create Contact Detail page at `src/app/(dashboard)/contacts/[id]/page.tsx` displaying timeline notes, company info, and assignment history.
- [ ] Create Companies view at `src/app/(dashboard)/companies/page.tsx`.
- [ ] Create CSV Import modal component in `src/components/crm/csv-import-modal.tsx`.

## Verification
- Run `pnpm typecheck` to verify UI components and page routes.
