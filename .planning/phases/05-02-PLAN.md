# Phase 5 Plan 2: tRPC Routers for Marketing Analytics & Task Reminders

## Objective
Build tRPC procedures for ad campaign ROI analytics, task scheduling/completion, and notification triggers.

## Tasks
- [ ] Build `src/server/api/routers/marketing.ts` with procedures for:
  - Ad campaign CRUD and performance metric calculation (CPL, CAC).
  - Creating, updating, and marking tasks completed.
  - Fetching user notification alerts.
- [ ] Register `marketingRouter` in `src/server/api/root.ts`.

## Verification
- Run `pnpm typecheck` to verify tRPC compilation.
