# Phase 3 Plan 1: Pipelines, Stages & Deals Database Schema

## Objective
Define Drizzle ORM schemas for `pipelines`, `pipeline_stages`, and `deals` with tenant organization isolation and stage ordering.

## Tasks
- [ ] Extend `src/server/db/schema.ts` with `pipelines`, `pipeline_stages`, and `deals` tables.
- [ ] Include fields for deal value, win probability, expected close date, order index, contact/company references, and assignee user IDs.

## Verification
- Run `pnpm typecheck` to verify schema type safety.
