# Phase 1 Plan 2: UI Design System & Component Foundation

## Objective
Initialize Shadcn UI and TailwindCSS v4 theme components (Button, Input, Card, Dialog, Table, DropdownMenu, Tabs, Form, Avatar) for the dashboard layout.

## Tasks
- [ ] Initialize Shadcn UI component configuration in `components.json`.
- [ ] Configure `src/styles/globals.css` with dark theme variables and Tailwind v4 utilities.
- [ ] Create core UI primitives in `src/components/ui/` (button, card, input, dialog, table, dropdown-menu, avatar, badge).
- [ ] Create navigation layout wrapper (`src/components/layout/dashboard-layout.tsx`) with sidebar navigation for Super Admin and Org Users.

## Verification
- Run `pnpm typecheck` and `pnpm check` to ensure zero lint or type errors.
