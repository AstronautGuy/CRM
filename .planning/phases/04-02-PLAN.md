# Phase 4 Plan 2: tRPC Routers for Catalog, Billing & Invoice Generation

## Objective
Build tRPC procedures for product catalog management, quote calculation/conversion, invoice tracking, and client subscriptions.

## Tasks
- [ ] Build `src/server/api/routers/billing.ts` with procedures for:
  - Product catalog CRUD and inventory stock adjustments.
  - Creating/updating quotes with line items, discounts, and taxes.
  - Converting quotes to invoices and tracking invoice payment statuses.
  - Client subscription management.
- [ ] Register `billingRouter` in `src/server/api/root.ts`.

## Verification
- Run `pnpm typecheck` to verify tRPC compilation.
