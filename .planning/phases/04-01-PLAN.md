# Phase 4 Plan 1: Products, Quotes, Invoices & Client Subscriptions Database Schema

## Objective
Define Drizzle ORM schemas for `products`, `quotes`, `quote_items`, `invoices`, `invoice_items`, and `client_subscriptions` with tenant organization isolation.

## Tasks
- [ ] Extend `src/server/db/schema.ts` with products, quotes, invoice tables, and line item schemas.
- [ ] Add Drizzle relations linking quotes and invoices to contacts, companies, and products.

## Verification
- Run `pnpm typecheck` to verify schema type safety.
