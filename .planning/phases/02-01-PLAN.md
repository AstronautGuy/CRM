# Phase 2 Plan 1: Contacts, Companies, Activity & Tags Database Schema

## Objective
Define Drizzle ORM schemas for Companies, Contacts, Lead Statuses, Custom Tags, Activity Notes, and Assignment History under tenant organization isolation.

## Tasks
- [ ] Extend `src/server/db/schema.ts` with `companies`, `contacts`, `tags`, `contact_tags`, `activity_notes`, and `assignment_history` tables with `organizationId` multi-tenant keys.
- [ ] Add Drizzle relations for Companies to Contacts, Contacts to Tags, and Activity Notes.

## Verification
- Run `pnpm typecheck` to verify schema type consistency.
