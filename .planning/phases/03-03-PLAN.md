# Phase 3 Plan 3: Interactive Drag-and-Drop Kanban Board UI

## Objective
Build interactive drag-and-drop Kanban Deal Board page with deal cards, stage columns, creation modals, and metrics summary widgets.

## Tasks
- [ ] Create Kanban Deal Board view at `src/app/pipeline/page.tsx`.
- [ ] Install or build drag-and-drop column card movement logic.
- [ ] Render top-level summary metrics cards (Total Pipeline Value, Forecasted MRR, Open Deals, Win Rate %).
- [ ] Add deal creation & edit modal component.

## Verification
- Run `pnpm typecheck` to verify UI components and page routing.
